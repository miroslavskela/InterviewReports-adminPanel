class Utils{
    static setButton = (a,b,c,d) => {
         if(!a || !b || !c || !d){
            return true
         }
         else{
             return false
         }
        
}
}

export default Utils